<DocsDemo as |demo|>
  <demo.example @name='service-errors-try-catch.hbs'>
    <p>Using a try/catch block with async/await</p>
    <div>
      <button
        {{on
          'click'
          (fn this.togglePlaySound 'https://invalid.example.url/1.mp3')
        }}
        type='button'
        class='ui-button'
      >
        {{#if this.sound.isPlaying}}
          Pause
        {{else}}
          Play Sound
        {{/if}}
      </button>
    </div>

    {{#if this.error}}
      <div class='text-danger text-sm px-1 py-2'>{{this.error}}</div>
    {{/if}}
  </demo.example>
  {{demo.snippet 'service-errors-try-catch.js'}}
  {{demo.snippet 'service-errors-try-catch.hbs'}}
</DocsDemo>