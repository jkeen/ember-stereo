<DocsDemo as |demo|>
  <demo.example @name="autoplay.hbs">
    {{#if (autoplay-allowed)}}
      Autoplay is allowed
    {{else}}
      Autoplay is not allowed
    {{/if}}
  </demo.example>
  {{demo.snippet "autoplay.hbs"}}
</DocsDemo>