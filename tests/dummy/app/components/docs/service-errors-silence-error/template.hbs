<DocsDemo as |demo|>
  <demo.example @name="service-errors-silence-errors.hbs">
    <p>You can silence the thrown errors and handle them inline</p>
    <div style="display: flex; justify-content: space-between;">
      <button {{on 'click' (fn this.togglePlaySound "https://invalid.example.url/1.mp3" )}} type="button" class="ui-button">
        {{#if this.sound.isPlaying}}
        Pause
        {{else}}
        Play Sound
        {{/if}}
      </button>
    </div>

    {{#if this.error}}
    <div style="color: red;">{{this.error}}</div>
    {{/if}}
  </demo.example>
  {{demo.snippet "service-errors-silence-errors.js"}}
  {{demo.snippet "service-errors-silence-errors.hbs"}}
</DocsDemo>